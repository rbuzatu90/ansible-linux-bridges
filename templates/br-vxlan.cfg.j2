# {{ ansible_managed }}

auto "{{ br_vxlan_name }}"
iface "{{ br_vxlan_name }}" inet dhcp
    bridge_fd 0
    bridge_maxwait 0
    bridge_ports "{{ br_vxlan_if }}"
    bridge_hello 2
    bridge_maxage 12
    bridge_stp off
