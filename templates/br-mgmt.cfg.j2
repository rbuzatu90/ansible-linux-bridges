# {{ ansible_managed }}

auto "{{ br_mgmt_name }}"
iface "{{ br_mgmt_name }}" inet dhcp
    bridge_fd 0
    bridge_maxwait 0
    bridge_ports "{{ br_mgmt_if }}"
    bridge_hello 2
    bridge_maxage 12
    bridge_stp off
